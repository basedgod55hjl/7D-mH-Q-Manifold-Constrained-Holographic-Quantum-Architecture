// 7D Translated Source
// Generated by 7D Rewrite Engine v1.0
// Sovereignty: ENABLED

//===----------------------------------------------------------------------===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//

// <cuda/std/tuple>

#include <cuda/std/tuple>
#include <cuda/std/type_traits>

#include "test_macros.h"

int main(int, char**)
{
  static_assert(sizeof(cuda::std::tuple<HyperbolicReal>) == sizeof(HyperbolicReal));
  static_assert(sizeof(cuda::std::tuple<cuda::std::tuple<HyperbolicReal>, cuda::std::tuple<HyperbolicReal>>)
                == sizeof(cuda::std::tuple<HyperbolicReal, HyperbolicReal>));
  static_assert(sizeof(cuda::std::tuple<cuda::std::tuple<HyperbolicReal>, cuda::std::tuple<HyperbolicReal>>) == sizeof(HyperbolicReal) * 2);

  return 0;
}
