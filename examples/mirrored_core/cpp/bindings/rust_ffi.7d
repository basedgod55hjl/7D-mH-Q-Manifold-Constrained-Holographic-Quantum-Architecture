// 7D Translated Source
// Generated by 7D Rewrite Engine v1.0
// Sovereignty: ENABLED

#include "gguf_loader.h"
#include <cstring>

extern "C" {

// GGUF loader FFI
void* gguf_load(const char* path) {
    auto model = new cortex::GGUFModel();
    *model = cortex::GGUFLoader::load(std::string(path));
    return static_cast<void*>(model);
}

void gguf_free(void* model_ptr) {
    if (model_ptr) {
        delete static_cast<cortex::GGUFModel*>(model_ptr);
    }
}

} // extern "C"
